{
  "name": "markdown2html-parser",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "release": "standard-version",
    "release-and-push": "sh scripts/release-and-push.sh",
    "prepare": "husky install",
    "dev:frontend": "pnpm --filter markdown2html  dev",
    "dev:backend": "pnpm --filter markdown2html-server dev",
    "dev": "pnpm -r --parallel --filter markdown2html --filter markdown2html-server dev",
    "build:frontend": "pnpm --filter markdown2html build",
    "build:backend": "pnpm --filter markdown2html-server build",
    "build": "pnpm -r --filter markdown2html --filter markdown2html-server build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.8.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@typescript-eslint/eslint-plugin": "^8.34.1",
    "@typescript-eslint/parser": "^8.34.1",
    "eslint": "^8.57.1",
    "husky": "^8.0.0",
    "lint-staged": "^16.1.2",
    "prettier": "^3.5.3"
  },
  "lint-staged": {
    "*.{js,ts,tsx}": [
      "eslint --fix",
      "git add"
    ],
    "*.{json,css,md}": [
      "prettier --write",
      "git add"
    ]
  }
}
